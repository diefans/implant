---

# test specs

localhost: !remote
    hostname: localhost


test: !group
    - !ref default:localhost

test: !group
    - !remote
        hostname: 127.0.0.1

foo:
    bar:
        baz: bam
        bim: boo
